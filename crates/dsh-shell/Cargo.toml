[package]
name = "dsh-shell"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "shell 接缝 + local 实现（P6，v1 前台执行）"

[dependencies]
dsh-core = { path = "../dsh-core" }
futures.workspace = true
thiserror.workspace = true

[dev-dependencies]
tokio.workspace = true
