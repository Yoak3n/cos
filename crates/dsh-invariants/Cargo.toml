[package]
name = "dsh-invariants"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "不变量注册表（模型可见 ⟺ 已记录、seq 单调等，P6）"

[dependencies]
dsh-core = { path = "../dsh-core" }
dsh-session = { path = "../dsh-session" }
