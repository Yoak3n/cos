[package]
name = "cos-system-prompt"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "prompt 段 + 工具 schema 装配（P5）"

[dependencies]
cos-core = { path = "../cos-core" }
serde_json.workspace = true
