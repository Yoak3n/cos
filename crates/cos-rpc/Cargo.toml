[package]
name = "cos-rpc"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "RPC 协议引擎（pi 对齐）：命令分发 + 流式事件投影 + JSONL 服务循环"

[dependencies]
cos-agent = { path = "../cos-agent" }
cos-core = { path = "../cos-core" }
cos-llm = { path = "../cos-llm" }
cos-session = { path = "../cos-session" }
futures.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
tokio.workspace = true

[dev-dependencies]
cos-agent-loop = { path = "../cos-agent-loop" }
cos-test-support = { path = "../cos-test-support" }
