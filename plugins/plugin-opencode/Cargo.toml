[package]
name = "plugin-opencode"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "OpenAI 兼容 chat/completions Provider 的运行时插件（声明式注册 kind: opencode）"

[dependencies]
cos-core = { path = "../../crates/cos-core" }
cos-llm = { path = "../../crates/cos-llm", features = ["openai"] }
cos-loader = { path = "../../crates/cos-loader" }
inventory.workspace = true
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
cos-test-support = { path = "../../crates/cos-test-support" }
futures.workspace = true
tokio.workspace = true
