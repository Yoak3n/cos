/**
 * @cos/sidecar — package root re-export of the JSON-RPC sidecar program.
 * Prefer the explicit subpaths (`./sidecar`, `./client`, `./server`, `./sea`).
 * @module @cos/sidecar
 */

export { main, serve } from './sidecar'
