/**
 * @cos/bundles/base — the "shared core" bundle aggregate.
 * @module @cos/bundles/base
 */

import type { Bundle } from '@cos/boot'

export const bundles: Array<string | Bundle> = ['@cos/bundle-base']

export default bundles